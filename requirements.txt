#
# Rayfield requirements (minimal, import-audited)
# ------------------------------------------------
# Notes:
# - PyTorch is CUDA/version-specific. On HPC it’s usually installed via conda modules.
#   If you *do* use pip, install torch from the official PyTorch index for your CUDA.
#

# Core training / sampling
numpy
torch
einops
findiff
matplotlib
tqdm
wandb
scipy

# Used by FDTD/neff utilities (optional)
# pandas
# meep            # typically conda-forge (pip often not ideal)
# tidy3d          # optional; install per Flexcompute instructions / license


