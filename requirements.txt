# PHASE — public requirements
# -----------------------------------------------------------------------------
# Core training, sampling, and evaluation dependencies.
# PyTorch is intentionally unpinned: install the wheel that matches your CUDA,
# e.g.  pip install torch --index-url https://download.pytorch.org/whl/cu121
# -----------------------------------------------------------------------------

numpy>=1.24
torch>=2.0
einops>=0.7
findiff>=0.10
scipy>=1.11
matplotlib>=3.7
tqdm>=4.65

# Optional: experiment tracking. Comment out if you don't use wandb.
wandb>=0.15

# -----------------------------------------------------------------------------
# Optional: dataset generation only. Skip these unless you plan to run
# FDTD/unified_sweep.py to regenerate the training dataset.
#
# Install Meep via conda (recommended):
#     conda install -c conda-forge pymeep
# Tidy3D is only used by some of the archived scripts under internal/ and
# requires a Flexcompute account.
# -----------------------------------------------------------------------------

# meep
# tidy3d
# pandas>=2.0
